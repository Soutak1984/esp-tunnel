[project]
name = "esp32-tunnel-server"
version = "2.0.0"
description = "Self-hosted tunnel relay server for ESP32"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "psutil>=5.9.0",
]

[dependency-groups]
dev = []
